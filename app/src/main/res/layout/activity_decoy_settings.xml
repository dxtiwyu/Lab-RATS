<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F2F2F7">

    <TextView
        android:id="@+id/settingsTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp"
        android:text="Settings"
        android:textColor="#000000"
        android:textSize="34sp"
        android:textStyle="bold"
        android:layout_marginTop="48dp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Fake iOS-style settings list -->
            <TextView style="@style/SettingsHeader" android:text="SYSTEM" />
            <LinearLayout style="@style/SettingsGroup">
                <TextView style="@style/SettingsItem" android:text="General" />
                <View style="@style/SettingsDivider" />
                <TextView style="@style/SettingsItem" android:text="Display &amp; Brightness" />
                <View style="@style/SettingsDivider" />
                <TextView style="@style/SettingsItem" android:text="Accessibility" />
            </LinearLayout>

            <TextView style="@style/SettingsHeader" android:text="PRIVACY &amp; SECURITY" />
            <LinearLayout style="@style/SettingsGroup">
                <TextView style="@style/SettingsItem" android:text="Privacy" />
                <View style="@style/SettingsDivider" />
                <TextView style="@style/SettingsItem" android:text="Security" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>

</LinearLayout>
